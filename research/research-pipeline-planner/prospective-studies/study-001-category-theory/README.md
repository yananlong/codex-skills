# Prospective Study 001: Category-Theory Repository Pilot

This directory is the draft execution pack for Issues #19, #17, and #15.

## State

- The three research cases are predeclared: `CT-ML`, `CT-NLP`, and `CT-FOUNDATIONS`.
- The public repository source environments are recorded at candidate commits.
- No case is enrolled.
- No hidden challenge set exists yet.
- No custodian or independent adjudicator is claimed.
- `conclusion_authority` remains `instrumentation_only`.

A passing structural validation shows only that the draft artifacts are internally consistent. It does not establish repository completeness, literature recall, scientific validity, or research-suite effectiveness.

## Public-source boundary

Every repository in `visible-source-registry.json` was named and inspected openly. Its content is visible input and cannot be counted as hidden challenge evidence. Exact file-level blob digests and mirror equivalence classes must be fixed before the protocol becomes `frozen`.

## Required next actions

1. Assign challenge custody and transition/source/omission adjudication.
2. Build an external challenge registry disjoint from all visible repository snapshots.
3. Freeze the exact relevant file set and blob digests.
4. Compute the canonical protocol digest.
5. Enroll cases only after freeze.
